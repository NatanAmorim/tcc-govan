name: govan
description: govan para passagueiros.
version: 0.1.0

# publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^2.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.4

flutter:
  assets:
    - assets/images/
    - assets/database/

  uses-material-design: true
